# ── Core Framework ────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0

# ── MCP (Model Context Protocol — Claude tool integration) ────
mcp==1.1.3
# Starlette must satisfy BOTH fastapi 0.115.x (>=0.40,<0.47) and mcp's SSE transport
starlette>=0.40.0,<0.47.0
sse-starlette>=1.6.1,<4.0.0
# Keep httpx in range that supabase/appwrite clients need
pydantic==2.10.6
pydantic-settings==2.7.1

# ── Database (Appwrite) ───────────────────────────────────────
appwrite>=5.0.0
httpx==0.27.0
httpcore>=1.0.0,<2.0.0
requests==2.32.5

# ── Security ───────────────────────────────────────────────────
python-multipart==0.0.27
cryptography>=46.0.7

# ── Email Service ─────────────────────────────────────────────
resend==2.0.0
email-validator==2.1.0

# ── Job Scheduling ────────────────────────────────────────────
apscheduler==3.10.4

# ── Redis + Celery (background tasks & distributed cache) ─────
redis>=5.0.0
celery[redis]>=5.3.6

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.2.2
loguru==0.7.2
pymupdf==1.25.3
Pillow>=10.0.0

# ── Development & Testing ─────────────────────────────────────
pytest==9.0.3
pytest-asyncio==0.21.1

# ── Viral Intel — RAG trend scouting ──────────────────
feedparser==6.0.11
praw==7.7.1
pytrends==4.9.2
beautifulsoup4>=4.12.0    # HTML parser for Browserless Google Search results
twikit>=2.2.0             # X/Twitter scraping via internal web API
curl-cffi>=0.7.0          # Chrome TLS impersonation — bypasses Cloudflare for twikit
httpx-curl-cffi>=0.1.0    # httpx transport for curl_cffi (patches twikit's HTTP client)
